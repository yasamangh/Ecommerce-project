import { PersianNumPipe } from './persian-num.pipe';

describe('PersianNumPipe', () => {
  it('create an instance', () => {
    const pipe = new PersianNumPipe();
    expect(pipe).toBeTruthy();
  });
});
